{
  "_comment": "TEMPORARY — smoke test of MinimumVersionGuard. minimumBuild 9999 is far above any real MinPin build so every installed copy will be blocked. Revert to 0 immediately after the test.",
  "minimumBuild": 9999,
  "minimumVersion": "9.9.9",
  "reason": "Smoke test of the MinimumVersionGuard kill switch. If you see this in MinPin, the guard is working — close it and wait a minute for it to be reverted."
}
